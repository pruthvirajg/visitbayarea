# Branches

| Branch | Role |
|---|---|
| **`main`** | Production. GitHub Pages deploys `site/` from here. |
| **`dev`** | Active work. New stops, research JSON, copy edits. |

```
feature/*  ->  dev  ->  main
```

Push research JSON to **`dev`**, not `main`. Merge into `src/lib/stops.ts` on `dev`.

Seller: **Pruthvi Studios**. App name: **Visit Bay Area**. Bundle ID: `studio.pruthvi.visitbayarea`.
